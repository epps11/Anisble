---
- hosts: Ubuntu
  

  tasks:
  - name: Upgrade all packages
    apt:
        name: '*'
        state: latest
  - name: Reboot a slow machine that might have lots of updates to apply
    reboot:
        reboot_timeout: 3600
